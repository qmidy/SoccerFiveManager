<UserControl x:Class="ConfigurationGameModule.ConfigurationGameView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <ResourceDictionary Source="pack://application:,,,/CommonLibrary;component/Resources/Colors.xaml"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="80"/>
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0"> Nom du Club </TextBlock>
            <TextBox Text="{Binding ClubName}" Grid.Column="1"/>
        </Grid>

        <Button Grid.Row="1" VerticalAlignment="Bottom" Click="CreateGameClick"> Commencer </Button>

    </Grid>
    
</UserControl>
