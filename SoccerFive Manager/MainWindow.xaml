<Window x:Class="Main.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Main"
        xmlns:common="clr-namespace:CommonLibrary;assembly=CommonLibrary"
        Title="MainWindow" Height="600" Width="800">
    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>
    <Window.Resources>
        <ResourceDictionary Source="pack://application:,,,/CommonLibrary;component/Resources/Colors.xaml"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Name="TopBanner" Height="{Binding TopBannerHeight}"/>
            <RowDefinition Name="ContentView" Height="*"/>
            <RowDefinition Name="BottomBanner" Height="{Binding BottomBannerHeight}"/>
        </Grid.RowDefinitions>
        <Rectangle Grid.Row="0" Visibility="{Binding IsTopBannerVisible}" Fill="{DynamicResource MiddleFadeYellow}"/>
        <ContentPresenter Grid.Row="1" Content="{Binding Path=MainContent}"/>
        <Rectangle Grid.Row="2" Visibility="{Binding IsBottomBannerVisible}" Fill="{DynamicResource MiddleFadeDarkGreen}"/>
    </Grid>
</Window>
